# Database connection string
# For local development: postgresql://user:password@localhost:5432/elevateballers?schema=public
# For production: Use connection string from your PostgreSQL provider (Vercel Postgres, Supabase, Neon, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/elevateballers?schema=public"

# JWT secret for authentication tokens
# Generate a strong random string: openssl rand -base64 32
# IMPORTANT: Use different secrets for development and production
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Environment
NODE_ENV="development"

# Deployment target (optional)
# Set to "vercel" for Vercel deployment, "cpanel" for cPanel deployment
# If not set, defaults to "cpanel" (Node.js adapter)
# DEPLOY_TARGET="cpanel"

