// Compatibility entrypoint for the admin route. The presentation is owned by
// the News feature, while CMS keeps the existing import contract.
export { default } from '../../../news/presentation/v2/AdminNewsListV2';
