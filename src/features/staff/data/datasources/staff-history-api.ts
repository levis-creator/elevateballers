import type { StaffRole } from "@prisma/client";
export type StaffAssignmentHistoryRecord = { id: string; teamId: string; staffId: string; role: StaffRole; effectiveFrom: string | null; effectiveTo: string | null; team: { id: string; name: string }; leagueSeason?: { id: string; status?: string | null; league?: { name?: string | null } | null; season?: { name?: string | null } | null } | null };
export type StaffTransferRecord = { id: string; staffId: string; fromTeamId: string; toTeamId: string; effectiveFrom: string; reason: string | null; createdAt: string; fromTeam: { id: string; name: string }; toTeam: { id: string; name: string } };
async function request<T>(url: string, init?: RequestInit): Promise<T> { const response = await fetch(url, { credentials: "same-origin", ...init }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || `Request failed (${response.status})`); return body as T; }
export const staffHistoryApi = { list: (id: string) => request<StaffAssignmentHistoryRecord[]>(`/api/staff/${id}/assignments?all=1`), transfers: (id: string) => request<StaffTransferRecord[]>(`/api/staff/${id}/transfers`), transfer: (id: string, data: { fromTeamStaffId: string; toTeamId: string; role: StaffRole; effectiveFrom: string; leagueSeasonId?: string; transferReason?: string }) => request<StaffTransferRecord>(`/api/staff/${id}/transfer`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }) };
