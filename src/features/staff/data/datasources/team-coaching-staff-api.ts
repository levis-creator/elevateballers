export type TeamCoachingStaffRecord = { id: string; name: string; role: string; type: "coach" | "manager" | "support"; email: string | null; photo: string | null; seasonId: string | null; active: boolean; sortOrder: number };
async function request<T>(url: string, init?: RequestInit): Promise<T> { const response = await fetch(url, { credentials: "same-origin", ...init }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || `Request failed (${response.status})`); return body as T; }
export const teamCoachingStaffApi = { list: (teamId: string) => request<TeamCoachingStaffRecord[]>(`/api/teams/${teamId}/coaching-staff?includeInactive=true`), save: (teamId: string, data: Record<string, unknown>, id?: string) => request<TeamCoachingStaffRecord>(`/api/teams/${teamId}/coaching-staff`, { method: id ? "PUT" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(id ? { ...data, id } : data) }), remove: (teamId: string, id: string) => request<void>(`/api/teams/${teamId}/coaching-staff?id=${encodeURIComponent(id)}`, { method: "DELETE" }) };
