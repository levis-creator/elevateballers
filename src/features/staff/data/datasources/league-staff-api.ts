export type LeagueStaffRecord = { id: string; name: string; role: string; department: string; email: string | null; photo: string | null; bio: string | null; active: boolean; sortOrder: number };
async function request<T>(url: string, init?: RequestInit): Promise<T> { const response = await fetch(url, { credentials: "same-origin", ...init }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || `Request failed (${response.status})`); return body as T; }
export const leagueStaffApi = { list: () => request<LeagueStaffRecord[]>("/api/league-staff?includeInactive=true"), create: (data: Omit<LeagueStaffRecord, "id">) => request<LeagueStaffRecord>("/api/league-staff", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }), update: (id: string, data: Partial<LeagueStaffRecord>) => request<LeagueStaffRecord>(`/api/league-staff/${id}`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }), remove: (id: string) => request<void>(`/api/league-staff/${id}`, { method: "DELETE" }) };
