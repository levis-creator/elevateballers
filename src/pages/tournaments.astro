---
export const prerender = false;

// The tournaments page has moved to /playoffs, which renders live brackets from
// real season data. Permanently redirect any old links.
return Astro.redirect('/playoffs/', 301);
---
