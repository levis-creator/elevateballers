---
export const prerender = false;
export const ssr = false;

export async function getStaticPaths() {
  return [];
}

import AdminLayout from '@/features/admin-layout/presentation/components/AdminLayout.astro';
import PageContentEditor from '../../../features/settings/components/PageContentEditor';

const { id } = Astro.params;
---

<AdminLayout title="Edit Page - Admin" permission="page_contents:update">
  <PageContentEditor pageId={id} client:load />
</AdminLayout>

