---
export const prerender = false;
export const ssr = false;

import AdminLayout from '../../../features/cms/components/AdminLayout.astro';
import GameRulesEditor from '../../../features/game-tracking/components/GameRulesEditor';

const { id } = Astro.params;
---

<AdminLayout title="Edit Game Rules - Admin">
  <div className="space-y-6">
    <div className="pb-6 border-b">
      <h1 className="text-3xl font-heading font-semibold mb-2">Edit Game Rules</h1>
      <p className="text-muted-foreground">Update game rule configuration</p>
    </div>
    <GameRulesEditor
      client:load
      rulesId={id}
      onSave={(rules) => {
        window.location.href = '/admin/game-rules';
      }}
    />
  </div>
</AdminLayout>
