---
export const prerender = false;
export const ssr = false;

export async function getStaticPaths() {
  return [];
}

import AdminLayout from '@/features/admin-layout/presentation/components/AdminLayout.astro';
import TeamView from '../../../../features/team/presentation/components/TeamView';

const { id } = Astro.params;
if (!id) {
  return Astro.redirect('/admin/teams', 302);
}
---

<AdminLayout title="View Team - Admin" permission="teams:read">
  <TeamView teamId={id} client:load />
</AdminLayout>

