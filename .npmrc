# npm configuration (project-level)
# Do not set prefix/global here - it breaks npx/prisma with "config prefix cannot be changed"
# On cPanel, if needed, set prefix in the server's .npmrc or env only.

# Performance optimizations
prefer-offline=true
audit=false
fund=false
loglevel=error

# Cache location
cache=${HOME}/.npm
